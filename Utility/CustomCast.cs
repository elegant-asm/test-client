
//using UnityEngine;

//namespace TestClient.Utility;
//internal static class CustomCast {
//    internal static bool Blockcast(Vector3 start, Vector3 end) {
//        if (!VUtil.isValidBBox(start, Controll.radius, 1))
//            return false;

//        // Check midpoint (lerp at 0.5)
//        Vector3 mid = Vector3.Lerp(start, end, 0.5f);
//        if (!VUtil.isValidBBox(mid, Controll.radius, 1))
//            return false;

//        // Check end point
//        if (!VUtil.isValidBBox(end, Controll.radius, 1))
//            return false;

//        return true;
//    }
//}
